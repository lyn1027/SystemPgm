셸(Shell)은 사용자와 운영체제의 심장인 커널 사이를 이어주는 명령어 해석기이며, 개발자에게는 시스템 제어와 자동화를 위한 필수 도구입니다.

구조 : 사용자(User) → 셸(Shell) → 커널(Kernel) → 하드웨어(Hardware)
주요 기능 : Command 해석, 파이프라인(`|`), 리다이렉션(`>`), 자동화 스크립트
대표 종류 : bash(표준), zsh(Mac/생산성), fish(편의성)
